import { cva } from "class-variance-authority";

export const codeVariants = cva(
  "bg-background  text-secondary-foreground relative rounded bg-muted px-[0.3rem] py-[0.2rem] font-mono text-sm font-semibold"
);
